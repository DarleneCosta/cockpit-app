import { View } from "react-native";
import Subtitle from "../../../components/Subtitle";
import styles from "./style";

const Address = ({ address }) => {
  return (
    <>
      <Subtitle text="ENDEREÇO DE COBRANÇA" />
      <View></View>
    </>
  );
};

export default Address;
